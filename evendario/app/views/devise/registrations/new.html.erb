<!--<div class="modal fade" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
<div class="signinbloc">
<h2> ¿Eres nuevo?</h2>
<h3>Registrate en menos de un minuto.</h3>

<div class= "formsignin">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-group">
   <%= f.email_field :fist_name, autofocus: true, placeholder: "Nombre", class: 'form-control form70' %>
  </div>

  <div class="form-group">
   <%= f.email_field :last_name, placeholder: "Apellido", class: 'form-control form70' %>
  </div>

  <div class="form-group">
   <%= f.email_field :email, placeholder: "Correo Electronico", class: 'form-control form70' %>
  </div>

  <div class="form-group">
    <% if @minimum_password_length %>
    <% end %>
    <%= f.password_field :password, autocomplete: "off", placeholder: "Contraseña (8 caracteres min)", class: 'form-control form70'%>
  </div>

  <div class="form-group">
    <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Confirma la contraseña", class: 'form-control form70'%>
  </div>



</div>
  <div class="actions">
    <%= f.submit "Sign up", class:"btn btn-success"%>
  </div>
<% end %>


<%= render "devise/shared/links" %>
</div>


</div> -->






<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">

      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">

           ¿Eres nuevo?<br>
          <p>Registrate en menos de un minuto.</p>

        </h4>
      </div>
      <div class="modal-body">
        <div class= "formsignin">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>

          <div class="form-group">
           <%= f.email_field :fist_name, autofocus: true, placeholder: "Nombre", class: 'form-control form70' %>
          </div>

          <div class="form-group">
           <%= f.email_field :last_name, placeholder: "Apellido", class: 'form-control form70' %>
          </div>

          <div class="form-group">
           <%= f.email_field :email, placeholder: "Correo Electronico", class: 'form-control form70' %>
          </div>

          <div class="form-group">
            <% if @minimum_password_length %>
            <% end %>
            <%= f.password_field :password, autocomplete: "off", placeholder: "Contraseña (8 caracteres min)", class: 'form-control form70'%>
          </div>

          <div class="form-group">
            <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Confirma la contraseña", class: 'form-control form70'%>
          </div>

      </div>
      <div class="modal-footer">
        <div class="actions">
          <%= f.submit "Sign up", class:"btn btn-success"%>
        </div>
      <% end %>
      <%= render "devise/shared/links" %>

      </div>
    </div>
  </div>
</div>
</div>
